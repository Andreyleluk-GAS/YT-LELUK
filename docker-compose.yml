version: '3.8'
services:
  backend:
    build: ./backend
    restart: always
    ports:
      - "3077:8080" # Оставляем для отладки

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "80:3000" # Сайт будет доступен сразу по IP без порта
    depends_on:
      - backend
